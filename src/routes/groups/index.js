import express from 'express'
// import { UsersBLL } from '../../logic/groups'

export const GroupsRoutes = express.Router()

/**
 * @swagger
 * /health:
 *  get:
 *    description: Get all users of the specific group by id
 *    responses:
 *      '200':
 *        description: Get all users of the specific group by id
 */
// GroupsRoutes.get('/users/group', UsersBLL.GetUsersByGroup)
